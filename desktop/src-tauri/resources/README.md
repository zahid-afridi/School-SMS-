# Generated by `npm run prepare:bundle` / `build.bat`
# Contains production builds only (app/backend dist, frontend standalone, openwa dist).
# Do not commit the large zip files — they are created on the build machine / CI.
