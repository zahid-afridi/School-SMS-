name: Release

on:
  push:
    tags: ['v*']

# Serialize ALL release runs on one global queue — deliberately not a per-tag group
# (`release-${{ github.ref_name }}`). The mutable channels (the X.Y minor tag and `latest`)
# are shared state across releases: a per-tag group would let two overlapping releases complete
# out of order, with the older build re-pointing those channels last. cancel-in-progress: false
# so a newer tag can never abort a promotion already in flight — a half-promoted registry state
# is worse than a queued release.
concurrency:
  group: release
  cancel-in-progress: false

env:
  NODE_VERSION: '22'
  DOCKER_PLATFORMS: linux/amd64,linux/arm64

permissions:
  contents: read

jobs:
  # ──── Release Gate (CI-strength) ──────────────────────────────────
  # Mirror the CI workflow's gate jobs so a tag can never publish an image or GitHub Release that
  # lint, the spec type-check, OpenAPI snapshot, unit + e2e tests, the Postgres migration smoke, or the
  # dashboard build/lint/type-check would have rejected. Each job below matches its CI counterpart; only the tag-match
  # guard and the publish jobs (docker, release) are release-specific.
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Fail fast (before tests, image build, or the GitHub Release) if the release is inconsistent:
      # the tag must match package.json, and the docs/CHANGELOG version guard must pass. This stops a
      # mis-tagged or under-documented release from ever publishing an image or a GitHub Release.
      - name: Verify tag matches package.json version
        env:
          TAG: ${{ github.ref_name }}
        run: |
          PKG_VERSION="$(node -p "require('./package.json').version")"
          TAG_VERSION="${TAG#v}"
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "::error::Tag '$TAG' (version '$TAG_VERSION') does not match package.json version '$PKG_VERSION'. Bump package.json or fix the tag before releasing."
            exit 1
          fi
          echo "Tag $TAG matches package.json $PKG_VERSION"

      - name: Security audit
        # Matches ci.yml's threshold: high (not critical). The release gate must not be laxer than the
        # PR gate, or a HIGH advisory blocked on every PR would still sail through at tag time.
        run: npm audit --audit-level=high

      - name: Run ESLint
        run: npm run lint

      # Same gap as CI: `nest build` excludes specs, so a spec-only type regression slips past every
      # existing release gate. tsconfig.json includes both src and test, so this is the full-program
      # check that also covers spec files.
      - name: Type-check full program (including specs)
        run: npx tsc --noEmit -p tsconfig.json

      - name: Check formatting
        run: npm run format:check

      - name: Check version consistency (docs track package.json)
        run: npm run check:versions

      # Same guard as CI: the published image is built from this ref, so the context must be sane.
      - name: Check Docker build context (.dockerignore)
        run: npm run check:dockerignore

      - name: Check OpenAPI snapshot is up to date
        run: npm run openapi:check

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test -- --coverage

      - name: Run e2e smoke tests
        run: npm run test:e2e

  test-postgres:
    name: Test (PostgreSQL migrations)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: openwa
          POSTGRES_PASSWORD: openwa
          POSTGRES_DB: openwa
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U openwa"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build (compiles the data migrations to dist/)
        run: npm run build

      # Applies the full data-migration chain to a real Postgres and asserts every generated-uuid PK
      # has a DB DEFAULT — the dialect gap SQLite-only tests can't see.
      - name: Migrate + uuid-default smoke against PostgreSQL
        run: npm run test:pg-smoke
        env:
          DATABASE_TYPE: postgres
          DATABASE_HOST: localhost
          DATABASE_PORT: '5432'
          DATABASE_USERNAME: openwa
          DATABASE_PASSWORD: openwa
          DATABASE_NAME: openwa

      # Runtime-proves BuiltInFtsProvider on Postgres (websearch_to_tsquery + ts_headline against the
      # STORED body_ts tsvector). The spec self-skips unless DATABASE_TYPE=postgres, so it is a no-op
      # in the default test job and only executes here against the postgres:16 service.
      - name: Postgres FTS provider spec
        run: npx jest src/database/migrations/__tests__/1782400000000-AddMessagesFts.pg.spec.ts
        env:
          DATABASE_TYPE: postgres
          DATABASE_HOST: localhost
          DATABASE_PORT: '5432'
          DATABASE_USERNAME: openwa
          DATABASE_PASSWORD: openwa
          DATABASE_NAME: openwa

  dashboard:
    name: Dashboard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: dashboard/package-lock.json

      - name: Install dependencies
        run: cd dashboard && npm ci

      - name: Run ESLint
        run: cd dashboard && npm run lint

      - name: Type-check dashboard tests
        run: cd dashboard && npm run typecheck

      - name: Check i18n parity
        run: cd dashboard && npm run i18n:check

      - name: Build dashboard
        run: cd dashboard && npm run build

      - name: Run dashboard unit tests
        run: cd dashboard && npm run test:unit

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test, dashboard]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

  # ──── Create GitHub Release ───────────────────────────────────────
  release:
    name: GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    # Gate the public Release on every CI-strength job AND a successfully built+pushed image that
    # also boots, so a tag never produces a GitHub Release without passing the same gate CI enforces,
    # a matching container image, and a runtime boot check on both architectures (boot-smoke runs the
    # published image on amd64 + arm64). Trades a few minutes of buildx/boot latency for release
    # safety.
    needs: [lint, test, test-postgres, dashboard, build, docker, boot-smoke, image-scan, promote, verify-published]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Extract release notes from CHANGELOG
        id: changelog
        run: |
          # Extract the section for this version from CHANGELOG.md
          VERSION="${{ steps.version.outputs.VERSION }}"
          NOTES=$(awk "/^## \[${VERSION}\]/{flag=1; next} /^## \[/{flag=0} flag" CHANGELOG.md)
          if [ -z "$NOTES" ]; then
            NOTES="Release v${VERSION}"
          fi
          # Use EOF delimiter for multi-line output
          {
            echo "NOTES<<EOF"
            echo "$NOTES"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        with:
          # Author the release as a real user (the PAT owner) instead of github-actions[bot].
          # Add a repo secret RELEASE_PAT — a fine-grained PAT scoped to this repo with
          # "Contents: read and write". Falls back to GITHUB_TOKEN (bot-authored) when the secret
          # is absent, so the job never breaks before the token is configured.
          token: ${{ secrets.RELEASE_PAT || secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref_name }}
          name: v${{ steps.version.outputs.VERSION }}
          body: ${{ steps.changelog.outputs.NOTES }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') }}

  # ──── Docker Image with Version Tag ───────────────────────────────
  docker:
    name: Docker Release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    needs: [lint, test, test-postgres, dashboard, build]
    # The promote and verify-published jobs consume exactly what this build produced — the pushed
    # manifest digest and the tag set computed once here — instead of re-deriving tags from the
    # tag pattern downstream. What is promoted and verified is by construction what was built.
    outputs:
      digest: ${{ steps.build.outputs.digest }}
      tags: ${{ steps.meta.outputs.tags }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Publish under a throwaway staging tag ONLY. The release tags (X.Y.Z, X.Y, latest) are
      # applied by the `promote` job, after boot-smoke has actually started the image on both
      # architectures. Publishing them here would make a broken image pullable before the test that
      # catches it has run — which is exactly how a non-starting image reached `latest` in 0.10.3.
      - name: Resolve staging ref
        id: staging
        env:
          REPO: ${{ github.repository }}
        run: echo "ref=ghcr.io/${REPO,,}:smoke-${{ github.run_id }}" >> "$GITHUB_OUTPUT"

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6
        with:
          # Both registries the promote job tags. Computed once here and handed to promote /
          # verify-published via job outputs, so the promoted set cannot drift from the build.
          images: |
            ghcr.io/${{ github.repository }}
            docker.io/rmyndharis/openwa
          tags: |
            type=semver,pattern={{version}}
            # The mutable channels (X.Y, latest) are disabled for prereleases: an rc/beta/alpha
            # build must never become what `docker pull openwa:0.11` or `:latest` resolves to.
            # The exact prerelease tag ({{version}}) is still published.
            type=semver,pattern={{major}}.{{minor}},enable=${{ !contains(github.ref_name, '-') }}
            type=raw,value=latest,enable=${{ !contains(github.ref_name, '-') }}

      - name: Build and push (staging tag only)
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          push: true
          platforms: ${{ env.DOCKER_PLATFORMS }}
          tags: ${{ steps.staging.outputs.ref }}
          labels: ${{ steps.meta.outputs.labels }}
          # provenance is generated by default (build-push-action v7); pin it explicitly and opt into
          # an SBOM attestation so each release image carries an in-toto SLSA provenance + SBOM pair,
          # verifiable via `docker buildx imagetools inspect`.
          provenance: true
          sbom: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Never serve the production stage's `apt-get update && apt-get install` from cache. That
          # layer's cache key is its own command text, which does not change when Debian publishes a
          # security update — so a cached hit silently installs whatever the apt index held when the
          # layer was first built, and the release image ships known-vulnerable packages. It is not a
          # theoretical risk: the first two v0.12.3 attempts installed chromium 150.0.7871.181 from a
          # cached layer while bookworm-security already carried 151.0.7922.71 on BOTH architectures,
          # and image-scan failed with 228 then 240 findings. The builder stage keeps its cache, so
          # only the OS-package layer is rebuilt.
          no-cache-filters: production

  # ──── Boot Smoke (amd64 + arm64) ──────────────────────────────────
  # The docker job proves the image BUILDS for both architectures; this proves the just-pushed image
  # actually BOOTS and serves the liveness endpoint on both. A boot regression that only surfaces at
  # runtime on one architecture (e.g. a native-dep/Chromium SIGTRAP on arm64 that a clean build still
  # produces) cannot reach a public Release. Runs the published image — not a rebuild — against the
  # dependency-free /api/health/live (NOT /ready, which needs a DB and would conflate boot failure
  # with database setup). Both architectures are tried even if the first fails, so logs name both.
  boot-smoke:
    name: Boot smoke (amd64 + arm64)
    runs-on: ubuntu-latest
    permissions:
      packages: read
    needs: [docker]
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve staging image ref
        id: image
        # The image under test carries only the staging tag at this point — the release tags are
        # applied by `promote`, after this job passes. ${REPO,,} lowercases github.repository: GHCR
        # normalizes the repo name to lowercase on push and `docker run` rejects a mixed-case
        # reference, so the pull target must be lowercase too (this repo is rmyndharis/OpenWA).
        env:
          REPO: ${{ github.repository }}
        run: echo "ref=ghcr.io/${REPO,,}:smoke-${{ github.run_id }}" >> "$GITHUB_OUTPUT"

      - name: Boot + liveness check on each architecture
        env:
          IMAGE: ${{ steps.image.outputs.ref }}
        run: |
          exit_code=0
          for platform in linux/amd64 linux/arm64; do
            echo "::group::boot-smoke $platform"
            docker rm -f openwa-smoke >/dev/null 2>&1 || true
            # Deliberately NOT --rm: a container that exits on its own is auto-removed by --rm, so
            # `docker logs` below finds nothing and the failure reports no cause. The explicit
            # `docker rm -f` at the end of each iteration handles cleanup instead.
            if ! docker run --platform "$platform" -d -p 2785:2785 --name openwa-smoke "$IMAGE" >/dev/null; then
              echo "FAIL: $platform — could not start container (image pull/run error)"
              exit_code=1
              echo "::endgroup::"
              continue
            fi
            ok=""
            for _ in $(seq 1 40); do
              if curl -sf http://127.0.0.1:2785/api/health/live >/dev/null 2>&1; then ok=1; break; fi
              sleep 5
            done
            if [ -n "$ok" ]; then
              echo "OK: $platform — /api/health/live returned 200"
            else
              echo "FAIL: $platform — /api/health/live did not return 200 within ~200s; container logs follow:"
              docker logs openwa-smoke 2>&1 | tail -60 || true
              exit_code=1
            fi
            docker stop openwa-smoke >/dev/null 2>&1 || true
            docker rm -f openwa-smoke >/dev/null 2>&1 || true
            echo "::endgroup::"
          done
          exit "$exit_code"

  # ──── Image Vulnerability Scan (amd64 + arm64) ────────────────────
  # Scans the just-pushed staging image for OS-package and library CVEs and blocks promotion of a
  # vulnerable image to the public release tags. This is the coverage the `npm audit` gate in ci.yml
  # cannot give: audit sees the npm tree, but the Debian packages baked into node:22-slim — and, on
  # arm64 only, the `chromium`/`chromium-sandbox` packages the amd64 image never installs (amd64 uses
  # a downloaded Chrome-for-Testing binary, not a .deb) — are visible only by scanning the built
  # artifact. Both architectures are scanned because their OS package sets diverge. Runs at release
  # time, not on every PR, so it never hits the fork-PR permission limits a push/PR scan would.
  # `ignore-unfixed` keeps a release from being blocked by a CVE with no fix yet; a fixable
  # HIGH/CRITICAL fails the job and — via promote's `needs` below — stops the release tags.
  image-scan:
    name: Image scan (${{ matrix.platform }})
    runs-on: ubuntu-latest
    permissions:
      packages: read
    needs: [docker]
    strategy:
      fail-fast: false
      matrix:
        platform: [linux/amd64, linux/arm64]
    steps:
      # Needed for .trivyignore: the scan reads the image from the registry, but the ignore file
      # is repo state and trivy-action resolves it from the workspace — without a checkout the
      # file simply is not there and every entry is silently inert.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Login to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve staging image ref
        id: image
        env:
          REPO: ${{ github.repository }}
        run: echo "ref=ghcr.io/${REPO,,}:smoke-${{ github.run_id }}" >> "$GITHUB_OUTPUT"

      - name: Trivy image scan
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        env:
          # trivy reads TRIVY_PLATFORM for its --platform flag. Scanning is static (it reads the
          # image layers, it does not run the image), so no QEMU is needed to inspect the arm64
          # variant of the manifest — only a pull, which the GHCR login above authorizes.
          TRIVY_PLATFORM: ${{ matrix.platform }}
        with:
          scan-type: image
          image-ref: ${{ steps.image.outputs.ref }}
          format: table
          vuln-type: os,library
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: '1'
          # Explicit rather than implicit: each entry in .trivyignore carries a written
          # justification and a condition for removing it.
          trivyignores: .trivyignore

  # Applies the release tags (X.Y.Z — plus X.Y and latest for non-prereleases) to the image
  # boot-smoke just proved can start, on BOTH registries (GHCR + Docker Hub — the tag set computed
  # once by the docker job's metadata-action covers both, and arrives here via job outputs, so the
  # promoted set is by construction the build's own tag set, never a re-derived pattern).
  # `imagetools create` re-points tags at the existing manifest list without rebuilding, so the
  # promoted image is bit-identical to the one tested on both registries, and its provenance and
  # SBOM attestations travel with it. If boot-smoke fails, this job never runs and the release
  # tags never appear anywhere.
  promote:
    name: Promote image to release tags
    runs-on: ubuntu-latest
    permissions:
      packages: write
    needs: [docker, boot-smoke, image-scan]
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Dual-publish: the same boot-tested manifest is also tagged on Docker Hub, where the public
      # pull count lives (GHCR exposes no download metrics). Requires the DOCKERHUB_USERNAME /
      # DOCKERHUB_TOKEN repo secrets; without them this step fails before any tag is applied.
      - name: Login to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Tag the smoke-tested image with the release tags
        env:
          REPO: ${{ github.repository }}
          REF_NAME: ${{ github.ref_name }}
          TAGS: ${{ needs.docker.outputs.tags }}
          EXPECTED_DIGEST: ${{ needs.docker.outputs.digest }}
        run: |
          set -euo pipefail
          staging="ghcr.io/${REPO,,}:smoke-${{ github.run_id }}"
          # Identity gate before anything is re-pointed: the staging manifest must be exactly the
          # manifest the docker job built (and boot-smoke/image-scan tested). A mismatch means the
          # tag under promotion is not the tested content, so stop.
          actual="$(docker buildx imagetools inspect "$staging" --format '{{.Manifest.Digest}}')"
          if [ "$actual" != "$EXPECTED_DIGEST" ]; then
            echo "::error::staging digest $actual != build digest $EXPECTED_DIGEST — refusing to promote untested content"
            exit 1
          fi
          # One tag per line. A here-string rather than a pipe, so the loop runs in this shell and
          # `set -e` actually aborts the step on a failed promotion instead of the pipeline
          # swallowing it.
          promoted=0
          while IFS= read -r tag; do
            [ -n "$tag" ] || continue
            # Defense in depth on top of the metadata `enable=` guards: a minor tag (X.Y) must
            # never be promoted from prerelease content, even if the tag set upstream changes.
            if [[ "$REF_NAME" == *-* ]] && [[ "$tag" =~ :[0-9]+\.[0-9]+$ ]]; then
              echo "skipping $tag — minor tags are not promoted from a prerelease"
              continue
            fi
            echo "promoting -> $tag"
            docker buildx imagetools create --tag "$tag" "$staging"
            # Each promoted tag must resolve to the build digest — the promotion is a re-point,
            # never new content.
            promoted_digest="$(docker buildx imagetools inspect "$tag" --format '{{.Manifest.Digest}}')"
            if [ "$promoted_digest" != "$EXPECTED_DIGEST" ]; then
              echo "::error::$tag resolved to $promoted_digest after promotion, expected $EXPECTED_DIGEST"
              exit 1
            fi
            promoted=$((promoted + 1))
          done <<< "$TAGS"
          if [ "$promoted" -eq 0 ]; then
            echo "::error::no release tags were produced by metadata-action — nothing was promoted"
            exit 1
          fi
          echo "promoted $promoted tag(s), all digest-identical to the tested build"

      - name: Verify the release tag resolves and is multi-arch
        env:
          REPO: ${{ github.repository }}
        run: |
          ref="ghcr.io/${REPO,,}:${GITHUB_REF#refs/tags/v}"
          docker buildx imagetools inspect "$ref" | tee /tmp/inspect.txt
          for want in linux/amd64 linux/arm64; do
            grep -q "$want" /tmp/inspect.txt || { echo "MISSING PLATFORM: $want"; exit 1; }
          done

  # ──── Staging tag cleanup (happy path AND failure path) ───────────
  # Best-effort tidy-up of the staging tag, and it MUST refuse to act unless that tag is the only
  # one on its version.
  #
  # GHCR deletes by *version*, not by tag, and a version carries every tag pointing at it. The
  # promote job re-points the release tags at content identical to the staging image, so they
  # resolve to the same digest and therefore the same version — whose tag list is
  # ["smoke-<run>", "X.Y.Z", "X.Y", "latest"]. Deleting that version by id took the release tags
  # with it: v0.10.5 published a GitHub Release while `latest`, `0.10` and `0.10.5` all vanished
  # from the registry, so `docker pull` failed for everyone until they were restored.
  #
  # This lives in its own `if: always()` job — not inside promote — because the failure path is
  # exactly when cleanup matters most: when boot-smoke or image-scan fails, promote is skipped and
  # the staging tag would otherwise leak. On the happy path the release tags share the staging
  # version, so the step below keeps it (the deliberate retention documented above); on a failed
  # run the staging tag is alone and gets removed.
  cleanup-staging:
    name: Clean up the staging tag
    runs-on: ubuntu-latest
    if: always()
    permissions:
      packages: write
    needs: [docker, boot-smoke, image-scan, promote]
    steps:
      - name: Remove the staging tag (only when it is alone on its version)
        # Best-effort: a failed tidy-up must not turn an otherwise successful release red.
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          staging="smoke-${{ github.run_id }}"
          version_json=$(gh api "/users/${{ github.repository_owner }}/packages/container/openwa/versions" \
            --jq ".[] | select(.metadata.container.tags[]? == \"$staging\") | {id, tags: .metadata.container.tags}" | head -1)
          if [ -z "$version_json" ]; then
            echo "staging tag not found (docker job failed before pushing, or already cleaned up)"
            exit 0
          fi
          version_id=$(printf '%s' "$version_json" | jq -r .id)
          tags=$(printf '%s' "$version_json" | jq -r '.tags | join(",")')
          tag_count=$(printf '%s' "$version_json" | jq -r '.tags | length')
          if [ "$tag_count" = "1" ]; then
            gh api -X DELETE "/users/${{ github.repository_owner }}/packages/container/openwa/versions/$version_id" \
              && echo "removed staging-only version $version_id"
          else
            echo "keeping version $version_id — the release tags share it (tags: $tags)."
            echo "Deleting it would unpublish them; the stray staging tag is left behind on purpose."
          fi

  # ──── Verify what the public can actually pull ────────────────────
  # The last thing to run, and the only check that looks at the registry the way a user does.
  #
  # v0.10.5 shipped with every job green and nothing pullable: the promote step tagged correctly, its
  # own verify step confirmed the tag, and the cleanup step immediately after deleted the version those
  # tags shared — so `latest`, `0.10` and `0.10.5` all vanished. Nothing noticed, because every existing
  # check ran BEFORE the cleanup and from a runner already logged in to GHCR.
  #
  # This job fixes both blind spots. It runs after promote (so it sees the final state, cleanup
  # included) and it never logs in, so it fails on a tag that exists but is not publicly readable —
  # which is indistinguishable from a missing tag to everyone downstream. The checked set is the
  # docker job's own tag output (both registries), so it matches whatever was actually promoted —
  # for a prerelease that is only the exact X.Y.Z-pre tag, with no stale expectation that `latest`
  # or X.Y moved. Every tag must also resolve to the build's digest, so a re-pointed channel can
  # never silently serve content other than the tested image.
  verify-published:
    name: Verify published tags (anonymous)
    runs-on: ubuntu-latest
    needs: [docker, promote]
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      # Deliberately NO docker/login-action here. Authenticating would defeat the purpose.
      - name: Resolve every promoted tag without credentials
        env:
          TAGS: ${{ needs.docker.outputs.tags }}
          EXPECTED_DIGEST: ${{ needs.docker.outputs.digest }}
        run: |
          # Fail closed. A verification step that reports success because something upstream of the
          # assertion broke is worse than no verification at all — `set -e` plus the checked-count
          # assertion below mean this job cannot pass without having actually resolved every
          # promoted tag.
          set -euo pipefail
          checked=0
          failed=0
          while IFS= read -r ref; do
            [ -n "$ref" ] || continue
            echo "::group::$ref"
            if ! docker buildx imagetools inspect "$ref" > /tmp/pub.txt 2>&1; then
              echo "::error::$ref is not publicly pullable after promotion."
              cat /tmp/pub.txt
              failed=1
              echo "::endgroup::"
              continue
            fi
            for want in linux/amd64 linux/arm64; do
              if ! grep -q "$want" /tmp/pub.txt; then
                echo "::error::$ref resolves but is missing $want"
                failed=1
              fi
            done
            digest="$(docker buildx imagetools inspect "$ref" --format '{{.Manifest.Digest}}')"
            if [ "$digest" != "$EXPECTED_DIGEST" ]; then
              echo "::error::$ref resolves to $digest, not the tested build digest $EXPECTED_DIGEST"
              failed=1
            fi
            echo "$ref OK ($digest)"
            checked=$((checked + 1))
            echo "::endgroup::"
          done <<< "$TAGS"
          if [ "$checked" -eq 0 ]; then
            echo "::error::no promoted tags were verified — treating that as a failed release."
            failed=1
          fi
          if [ "$failed" -ne 0 ]; then
            echo "::error::The release is not usable. Re-point the missing tags before announcing it."
            exit 1
          fi
          echo "all $checked release tags are publicly pullable, multi-arch, and digest-identical to the tested build"
